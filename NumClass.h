

// 0 for false
// 1 for true

// will return if a number is armstrong number, armstrong number is an n digit number that is equal to the sum of the nth powers of its digit
int isArmstrong(int);

// number that reads the same forward as backward
int isPalindrome(int);

//prime number
int isPrime(int);

// strong number is a special number whose sum of the factorial(!) of digits is equal to the original number
int isStrong(int);
